<%= form_for(@fake_process) do |f| %>
  <% if @fake_process.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fake_process.errors.count, "error") %> prohibited this fake_process from being saved:</h2>

      <ul>
      <% @fake_process.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :done %><br>
    <%= f.check_box :done %>
  </div>
  <div class="actions">
    <%= f.submit, class: 'btn'%>
  </div>
<% end %>